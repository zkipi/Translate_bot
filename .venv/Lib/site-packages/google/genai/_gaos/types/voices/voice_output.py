# Copyright 2026 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# pyformat: disable
# pylint: skip-file

"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .audiodata import AudioData, AudioDataParam
from .pitch import Pitch
from .promptedvoice import PromptedVoice, PromptedVoiceParam
from .voicetype import VoiceType
from datetime import datetime
from .. import BaseModel, UNSET_SENTINEL
from ..interactions import usage as interactions_usage
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class VoiceOutputTypedDict(TypedDict):
    r"""A voice resource representing either a custom voice (created via
    `CreateVoice`) or a prebuilt system voice (returned by `ListVoices`).
    """

    type: VoiceType
    accent: NotRequired[str]
    r"""Optional. Regional accent descriptor (e.g. \"American\", \"British\")."""
    context: NotRequired[str]
    r"""Optional. Optimal usage context or domain (e.g. \"Conversational\",
    \"Audiobook\", \"News\").
    """
    description: NotRequired[str]
    r"""Optional. Descriptive summary of vocal timbre, personality, and tone."""
    display_name: NotRequired[str]
    r"""Optional. User-provided display name for a stored voice (`store = true`),
    or the catalog name for a prebuilt voice.
    """
    expire_time: NotRequired[datetime]
    r"""Output only. The timestamp at which a custom stored voice (`store = true`)
    or replicated voice key (`store = false`) expires. Unset for prebuilt
    catalog voices (`\"prebuilt\"`), which do not expire.
    """
    gender: NotRequired[str]
    r"""Optional. Perceived voice gender presentation (e.g. \"female\", \"male\",
    \"neutral\").
    """
    id: NotRequired[str]
    r"""Output only. The unique identifier of the voice.

    * For Google-managed custom voices (`store = true`), this is a generated ID
    with prefix `voice_` (for example, `voice_abc123def456`). Pass
    `voices/{id}` as the `name` in `GetVoice` and `DeleteVoice`, and pass
    `{id}` directly to `SpeechConfig.voice_config.voice` (or
    `SpeechConfig.voice`) during speech synthesis.
    * For prebuilt catalog voices (`\"prebuilt\"` returned by `ListVoices`), this
    is the speaker name (for example, `Puck` or `Charon`).
    * Unset when `CreateVoice` is called with `store = false`.
    """
    key: NotRequired[str]
    r"""Output only. The client-managed voice replication key (with prefix
    `voicekey_`). Returned only by `CreateVoice` when `type` is `\"replicated\"`
    and `store` is `false`. Pass this key to `SpeechConfig.voice_config.voice`
    (or `SpeechConfig.voice`) during speech synthesis.
    """
    language_code: NotRequired[str]
    r"""Optional. Primary BCP-47 language tag (e.g. \"en-US\", \"fr-FR\")."""
    model: NotRequired[str]
    r"""Optional. The model used to design or replicate the voice.
    If omitted in `CreateVoice`, defaults to the latest supported voice design
    model. Returned in `CreateVoice`, `GetVoice`, and `ListVoices` responses
    for custom voices (`\"prompted\"` and `\"replicated\"`); unset for `\"prebuilt\"`
    voices. Created voices can be synthesized across any supported TTS
    synthesis model.
    """
    persona: NotRequired[str]
    r"""Optional. Intended persona or character archetype (e.g. \"Warm, Friendly\",
    \"Narrator\").
    """
    pitch: NotRequired[Pitch]
    prompted: NotRequired[PromptedVoiceParam]
    r"""Parameters for prompted voice generation.
    Required in `CreateVoice` when `type` is `\"prompted\"`. Returned in
    `CreateVoice`, `GetVoice`, and `ListVoices` responses for prompted voices.
    """
    region_code: NotRequired[str]
    r"""Optional. ISO 3166-1 alpha-2 or UN M.49 geographic region code (e.g. \"US\",
    \"GB\", \"001\").
    """
    sample_audio: NotRequired[AudioDataParam]
    r"""Audio payload used for voice creation."""
    usage: NotRequired[interactions_usage.UsageTypedDict]
    r"""Statistics on the interaction request's token usage."""


class VoiceOutput(BaseModel):
    r"""A voice resource representing either a custom voice (created via
    `CreateVoice`) or a prebuilt system voice (returned by `ListVoices`).
    """

    type: VoiceType

    accent: Optional[str] = None
    r"""Optional. Regional accent descriptor (e.g. \"American\", \"British\")."""

    context: Optional[str] = None
    r"""Optional. Optimal usage context or domain (e.g. \"Conversational\",
    \"Audiobook\", \"News\").
    """

    description: Optional[str] = None
    r"""Optional. Descriptive summary of vocal timbre, personality, and tone."""

    display_name: Optional[str] = None
    r"""Optional. User-provided display name for a stored voice (`store = true`),
    or the catalog name for a prebuilt voice.
    """

    expire_time: Optional[datetime] = None
    r"""Output only. The timestamp at which a custom stored voice (`store = true`)
    or replicated voice key (`store = false`) expires. Unset for prebuilt
    catalog voices (`\"prebuilt\"`), which do not expire.
    """

    gender: Optional[str] = None
    r"""Optional. Perceived voice gender presentation (e.g. \"female\", \"male\",
    \"neutral\").
    """

    id: Optional[str] = None
    r"""Output only. The unique identifier of the voice.

    * For Google-managed custom voices (`store = true`), this is a generated ID
    with prefix `voice_` (for example, `voice_abc123def456`). Pass
    `voices/{id}` as the `name` in `GetVoice` and `DeleteVoice`, and pass
    `{id}` directly to `SpeechConfig.voice_config.voice` (or
    `SpeechConfig.voice`) during speech synthesis.
    * For prebuilt catalog voices (`\"prebuilt\"` returned by `ListVoices`), this
    is the speaker name (for example, `Puck` or `Charon`).
    * Unset when `CreateVoice` is called with `store = false`.
    """

    key: Optional[str] = None
    r"""Output only. The client-managed voice replication key (with prefix
    `voicekey_`). Returned only by `CreateVoice` when `type` is `\"replicated\"`
    and `store` is `false`. Pass this key to `SpeechConfig.voice_config.voice`
    (or `SpeechConfig.voice`) during speech synthesis.
    """

    language_code: Optional[str] = None
    r"""Optional. Primary BCP-47 language tag (e.g. \"en-US\", \"fr-FR\")."""

    model: Optional[str] = None
    r"""Optional. The model used to design or replicate the voice.
    If omitted in `CreateVoice`, defaults to the latest supported voice design
    model. Returned in `CreateVoice`, `GetVoice`, and `ListVoices` responses
    for custom voices (`\"prompted\"` and `\"replicated\"`); unset for `\"prebuilt\"`
    voices. Created voices can be synthesized across any supported TTS
    synthesis model.
    """

    persona: Optional[str] = None
    r"""Optional. Intended persona or character archetype (e.g. \"Warm, Friendly\",
    \"Narrator\").
    """

    pitch: Optional[Pitch] = None

    prompted: Optional[PromptedVoice] = None
    r"""Parameters for prompted voice generation.
    Required in `CreateVoice` when `type` is `\"prompted\"`. Returned in
    `CreateVoice`, `GetVoice`, and `ListVoices` responses for prompted voices.
    """

    region_code: Optional[str] = None
    r"""Optional. ISO 3166-1 alpha-2 or UN M.49 geographic region code (e.g. \"US\",
    \"GB\", \"001\").
    """

    sample_audio: Optional[AudioData] = None
    r"""Audio payload used for voice creation."""

    usage: Optional[interactions_usage.Usage] = None
    r"""Statistics on the interaction request's token usage."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "accent",
                "context",
                "description",
                "display_name",
                "expire_time",
                "gender",
                "id",
                "key",
                "language_code",
                "model",
                "persona",
                "pitch",
                "prompted",
                "region_code",
                "sample_audio",
                "usage",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
